services:
  db:
    image: mysql:8.0
    container_name: mysql-container
    restart: always
    environment:
      MYSQL_DATABASE: meu_banco
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3307:3306"